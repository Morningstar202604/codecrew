# CodeCrew 项目总规划

> **版本**: v0.0.1 (MVP)  
> **定位**: 终端原生、多角色协作、可扩展的 AI 编程助手  
> **核心理念**: 像指挥一个开发团队一样指挥 AI —— 有分工、有流程、有质检

---

## 🎯 产品愿景

**一句话**：把 AI 从「聊天机器人」变成「可编程的开发团队」。

- **角色化**：开发者、审查员、架构师、测试员……各司其职
- **流程化**：需求 → 设计 → 编码 → 审查 → 测试 → 文档，全链路自动化
- **可配置**：模型、工具、权限、提示词全在配置文件里，想怎么玩就怎么玩
- **本地优先**：数据不出本机，支持全离线模型（Ollama），隐私可控

---

## 🏗️ 架构总览

```
CodeCrew (单二进制)
├── CLI 交互层
│   ├── REPL 主循环（角色提示符、流式输出、命令系统）
│   ├── 配置向导（三步搞定：复制模板 → 填密钥 → /reload）
│   └── 多语言命令别名（/role = 角色，/model = 模型）
├── 核心引擎
│   ├── 角色引擎（frontmatter 解析、工具白名单、系统提示词注入）
│   ├── 工具注册表（read/write/edit/bash + 权限闸门）
│   ├── LLM 客户端（OpenAI 兼容、Function Calling、流式累积）
│   └── 会话管理（历史、上下文窗口、工具调用闭环）
├── 配置系统
│   ├── 分层加载：项目级 codecrew.json > 全局 ~/.codecrew/config.json > 环境变量
│   ├── 多供应商：DeepSeek / Qwen / Kimi / GLM / OpenAI / Ollama
│   └── 密钥脱敏、模板示例、热重载
└── 角色库（内置 + 用户自定义）
    ├── developer（全栈开发）
    ├── reviewer（代码审查）
    ├── architect（架构设计，规划中）
    ├── tester（测试生成/执行，规划中）
    └── docs（文档生成，规划中）
```

---

## ✅ v0.0.1 当前完成度（MVP）

| 模块           | 状态 | 关键能力                                                            |
| -------------- | ---- | ------------------------------------------------------------------- |
| **CLI 框架**   | ✅   | 友好欢迎页、命令系统、角色/模型热切换、配置热重载                   |
| **配置系统**   | ✅   | 项目级/全局级 codecrew.json、多供应商、密钥脱敏、模板示例           |
| **角色系统**   | ✅   | Markdown+frontmatter、工具白名单、热切换、系统提示词注入            |
| **LLM 客户端** | ✅   | OpenAI 兼容流式、Function Calling(tool_calls累积)、多供应商通吃     |
| **工具基建**   | ✅   | read/write/edit/bash 四件套、注册表、权限闸门、参数校验、安全路径   |
| **Agent 闭环** | ✅   | 用户输入 → LLM流式 → tool_calls累积 → 工具执行 → 结果回填 → 再次LLM |

---

## 🗺️ 后续路线图

### v0.1.0 - 可用于日常开发（核心闭环完善）

| 任务             | 优先级 | 预估工时 | 说明                                               |
| ---------------- | ------ | -------- | -------------------------------------------------- |
| **权限交互确认** | P0     | 2d       | bash/write/edit 需用户确认；白名单/黑名单/交互三档 |
| **规划工具**     | P0     | 3d       | 内置 `plan`/`todo` 工具，LLM 能拆解任务、跟踪进度  |
| **上下文管理**   | P1     | 2d       | 自动摘要、文件引用去重、token 预算预警             |
| **会话持久化**   | P1     | 1d       | SQLite 存储会话历史、支持断点续聊                  |
| **架构师角色**   | P1     | 2d       | 只读+规划工具，输出设计文档/任务拆解               |
| **测试员角色**   | P1     | 2d       | 读代码→生成测试→跑测试→修复循环                    |

### v0.2.0 - 团队化协作（多角色流水线）

| 任务           | 优先级 | 预估工时 | 说明                                                                               |
| -------------- | ------ | -------- | ---------------------------------------------------------------------------------- |
| **流水线编排** | P0     | 3d       | `/pipeline 实现X` → architect拆解 → developer写 → reviewer审 → tester测 → 自动循环 |
| **圆桌模式**   | P1     | 2d       | 多角色就同一设计辩论 N 轮再动手                                                    |
| **角色记忆**   | P1     | 2d       | 每角色维护长期笔记（架构师记决策、测试员记坑点）                                   |
| **角色市场**   | P2     | 3d       | `crew role install xxx` 一键安装社区角色包                                         |

### v0.3.0 - 生产级打磨

| 任务             | 优先级 | 预估工时 | 说明                                     |
| ---------------- | ------ | -------- | ---------------------------------------- |
| **权限三档制**   | P0     | 2d       | 允许/询问/拒绝 + 危险命令识别 + 审计日志 |
| **Token 可视化** | P1     | 1d       | 实时显示上下文占用、预估成本             |
| **插件系统**     | P2     | 3d       | Go 插件 / WASM，用户可自定义工具         |
| **多语言 i18n**  | P2     | 2d       | 中英日韩界面切换                         |
| **基准测试套件** | P1     | 2d       | SWE-bench / HumanEval 自动跑分           |

### v1.0.0 - 正式发布

- 完整文档、教程、视频演示
- 社区角色市场上线
- 稳定 API、语义化版本、发布流程自动化

---

## 🛠️ 技术栈与约束

| 维度     | 选择                        | 理由                                             |
| -------- | --------------------------- | ------------------------------------------------ |
| 语言     | Go 1.22+                    | 单二进制、跨平台、机械硬盘友好、生态成熟         |
| LLM 协议 | OpenAI 兼容                 | 生态最广（DeepSeek/Qwen/Kimi/GLM/Ollama 全支持） |
| 配置格式 | JSON                        | 标准库支持、注释可通过 example 说明、工具链友好  |
| 角色定义 | Markdown + YAML frontmatter | 人类可读可写、版本控制友好、无需额外解析器       |
| 存储     | 文件系统 + 可选 SQLite      | 零依赖、可迁移、可选升级                         |
| 打包     | 单二进制 (go build)         | 零运行时依赖、分发极简                           |

---

## 📦 仓库结构规范

```
codecrew/
├── .github/
│   ├── workflows/          # CI/CD
│   └── ISSUE_TEMPLATE/     # Issue 模板
├── cmd/
│   └── codecrew/           # 入口 main.go
├── internal/
│   ├── config/             # 配置加载/解析
│   ├── llm/                # LLM 客户端
│   ├── role/               # 角色加载/管理
│   └── tool/               # 工具注册表/实现
├── roles/                  # 内置角色
│   ├── developer.md
│   ├── reviewer.md
│   ├── architect.md
│   ├── tester.md
│   └── docs.md
├── scripts/                # 构建/发布脚本
├── testdata/               # 测试数据
├── go.mod / go.sum
├── codecrew.example.json   # 配置模板
├── .gitignore
├── LICENSE
├── README.md
└── PLAN.md                 # 本文件
```

---

## 🚀 快速开始（给用户）

```bash
# 1. 下载单二进制（或 go install）
go install github.com/yourname/codecrew@latest

# 2. 三步配置
cp codecrew.example.json codecrew.json
# 编辑 codecrew.json 填入 API Key
codecrew /reload

# 3. 开始用
codecrew
> 帮我写个 Go HTTP 服务器
```

---

## 📝 变更日志

### v0.0.1 (2026-08-27)

- 初始 MVP：REPL、角色、配置、LLM流式、工具闭环
- 角色：developer / reviewer
- 工具：read / write / edit / bash
- 配置：codecrew.json + 全局配置 + 环境变量兜底
- 多供应商：DeepSeek / Qwen / Kimi / GLM / OpenAI / Ollama

---

## 🤝 贡献指南

1. Fork → 分支 → PR
2. 遵循 `internal/*` 分层，禁止跨层直接依赖
3. 新工具必须实现 `tool.Tool` 接口并在 `registry.go` 注册
4. 新角色放入 `roles/*.md`，遵循 frontmatter 规范
5. 所有用户可见文本走 i18n key（后续版本）

---

> **维护者提示**：本文件作为项目总纲，每个里程碑版本发布前必须同步更新。详细任务拆解见 GitHub Projects / Issues。
